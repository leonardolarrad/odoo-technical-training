<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
        <record id="academy_sale_wizard_from_view" model="ir.ui.view">
            <field name="name">sale.order.form.inherit.academy</field>
            <field name="model">academy.sale.wizard</field>
            <field name="arch" type="xml">
                <form string="Wizard">
                    <group>
                        <field name="session_id"/>
                        <field name="session_student_ids" invisible="1"/>
                        <field 
                            name="student_ids" 
                            domain="[('id', 'in', session_student_ids)]"
                        />
                    </group>
                    <footer>
                        <button 
                            name="create_sale_orders" 
                            string="Crear" 
                            type="object" 
                            class="oe_highlight"
                        />
                        <button 
                            string="Cancel" 
                            special="cancel"
                        />
                    </footer>
                </form>
            </field>
        </record>

        <!--
        <act_window 
            id="launch_sale_wizard"
            name="Crear orden de venta"
            binding_model="academy.session"
            res_model="academy.sale.wizard"
            view_mode="form"
            target="new"
        />
        -->

        <record id="launch_sale_wizard" model="ir.actions.act_window">
            <field name="name">Crear orden de venta</field>
            <field name="binding_model_id" ref="academy.model_academy_session"/>
            <field name="res_model">academy.sale.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data> 
</odoo>